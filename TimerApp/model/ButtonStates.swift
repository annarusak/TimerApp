import Foundation

    enum StartPauseButtonState: String {
        case start = "Start"
        case stop = "Stop"
    }
    
    enum LapResetButtonState: String {
        case unenabledLap
        case lap = "Lap"
        case reset = "Reset"
    }

